import { createContext } from 'react';
import 'styles/global.scss';

const defaultContext = createContext({});
const App = () => {
  return (
    <div className="App">
    </div>
  );
}

export default App;
